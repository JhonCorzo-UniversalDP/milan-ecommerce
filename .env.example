DATABASE_URL="file:./dev.db"

# Catalog source. Supported: "json" (default), "odoo" (stub).
CATALOG_SOURCE=json

# Path to the JSON catalog, relative to the project root or absolute.
CATALOG_JSON_PATH=data/product_template.json
